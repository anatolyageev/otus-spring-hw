
quiz:
    file: quiz/quiz
    locale: en

spring:
    messages:
        basename: quiz_interface
        encoding: utf-8
        fallback-to-system-locale: false
    shell:
        interactive:
             enabled: false